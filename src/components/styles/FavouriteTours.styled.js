import { styled } from 'styled-components'

export const FavouriteToursContainer = styled.div`
  margin: 100px auto;
`
export const FavouriteToursHeader = styled.div`
  text-align: right;

  /* @media (max-width: 1200px) {
    text-align: center;
  } */
`
export const ClearAllTitle = styled.span`
  font-size: 14pt;
  &:hover {
    cursor: pointer;
  }
`
